name: TestGird Checker
description: 'Check the TestGrid status for pull requests'
inputs:
  GITHUB_TOKEN:
    required: true
    description: GitHub token
runs:
  using: 'node16'
  main: 'dist/index.js'