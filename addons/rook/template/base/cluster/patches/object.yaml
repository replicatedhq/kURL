---
apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: rook-ceph-store
  namespace: rook-ceph
spec:
  gateway:
    resources:
      requests:
        cpu: "300m"
        memory: "1024Mi"
