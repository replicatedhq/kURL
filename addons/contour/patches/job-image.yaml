---
apiVersion: batch/v1
kind: Job
metadata:
  name: contour-certgen-v
  namespace: projectcontour
spec:
  template:
    spec:
      containers:
      - name: contour
        image: projectcontour/contour:v
        imagePullPolicy: IfNotPresent
