- name: "Velero Minimal"
  cpu: 6
  installerSpec:
    kubernetes:
      version: "1.33.x"
    flannel:
      version: latest
    containerd:
      version: "2.2.x"
    minio:
      version: "2025-10-15T17-29-55Z"
    kotsadm:
      version: latest
    openebs:
      version: "4.4.x"
      isLocalPVEnabled: true
      localPVStorageClassName: "local"
    velero:
      version: "__testver__"
      s3Override: "__testdist__"
  preInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    # BEGIN GENERATED os-matrix: ubuntu-host-packages — edit os-matrix.yaml, run 'make generate-os-matrix'
    ubuntu_2404_install_host_packages nfs-common
    ubuntu_2604_install_host_packages nfs-common
    # END GENERATED os-matrix: ubuntu-host-packages
  postInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    install_and_customize_kurl_integration_test_application
    timeout 5m kubectl kots backup
    timeout 5m kubectl kots backup ls

    # Check if the support bundle spec was installed
    echo "test whether the velero support bundle spec was installed"
    supportBundle=$(kubectl get cm -n velero kurl-velero-supportbundle-spec -ojsonpath='{.data.support-bundle-spec}')
    echo "$supportBundle"
    echo "test if the support bundle has 'troubleshoot.io/kind: support-bundle' label"
    kubectl get cm -n velero kurl-velero-supportbundle-spec -oyaml | grep 'troubleshoot.io/kind: support-bundle'
  unsupportedOSIDs:
    - centos-79
    - ol-79
    - ol-8x
    - amzn-20
    - ubuntu-1804

- name: "Velero Minimal Airgap"
  cpu: 6
  airgap: true
  installerSpec:
    kubernetes:
      version: "1.31.x"
    flannel:
      version: latest
    rook:
      version: 1.12.x
    containerd:
      version: "1.7.x"
    kotsadm:
      version: latest
    registry:
      version: latest
    velero:
      version: "__testver__"
      s3Override: "__testdist__"
  preInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    rhel_9_install_host_packages lvm2 conntrack-tools socat container-selinux git
    # BEGIN GENERATED os-matrix: ubuntu-host-packages — edit os-matrix.yaml, run 'make generate-os-matrix'
    ubuntu_2404_install_host_packages nfs-common containerd socat ebtables conntrack lvm2
    ubuntu_2604_install_host_packages nfs-common containerd socat ebtables conntrack lvm2
    # END GENERATED os-matrix: ubuntu-host-packages
  postInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    install_and_customize_kurl_integration_test_application
    timeout 5m kubectl kots backup
    timeout 5m kubectl kots backup ls

- name: "Velero upgrade"
  cpu: 8
  installerSpec:
    kubernetes:
      version: "1.33.x"
    flannel:
      version: latest
    containerd:
      version: "1.7.x"
    minio:
      version: "2025-10-15T17-29-55Z"
    kotsadm:
      version: latest
    openebs:
      version: "4.4.x"
      isLocalPVEnabled: true
      localPVStorageClassName: "local"
    velero:
      version: "1.16.x"
  upgradeSpec:
    kubernetes:
      version: "1.33.x"
    flannel:
      version: latest
    containerd:
      version: "1.7.x"
    minio:
      version: "2025-10-15T17-29-55Z"
    kotsadm:
      version: latest
    openebs:
      version: "4.4.x"
      isLocalPVEnabled: true
      localPVStorageClassName: "local"
    velero:
      version: "__testver__"
      s3Override: "__testdist__"
  preInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    # BEGIN GENERATED os-matrix: ubuntu-host-packages — edit os-matrix.yaml, run 'make generate-os-matrix'
    ubuntu_2404_install_host_packages nfs-common lvm2
    ubuntu_2604_install_host_packages nfs-common lvm2
    # END GENERATED os-matrix: ubuntu-host-packages
    rhel_9_install_host_packages lvm2 conntrack-tools socat container-selinux git
  postInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    install_and_customize_kurl_integration_test_application
    timeout 5m kubectl kots backup
    timeout 5m kubectl kots backup ls
  postUpgradeScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    kubectl get pods -n velero
    timeout 5m kubectl kots backup
    timeout 5m kubectl kots backup ls
    # if restic pods are still running, fail the test
    if kubectl get pods -n velero | grep restic; then
      echo "Restic pods still running after upgrade"
      exit 1
    fi

# Reproduces the kotsadm <= 1.131.5 bug where KOTS configures Velero onto the Local Volume
# Provider even when Minio is present, then upgrades to a Velero version that no longer
# supports the LVP. The upgrade must migrate the default BackupStorageLocation to the
# in-cluster object store instead of blocking.
- name: "Velero LVP Upgrade"
  cpu: 8
  # auto-accept the Local Volume Provider migration confirmation prompt
  flags: "yes"
  installerSpec:
    kubernetes:
      version: "1.33.x"
    flannel:
      version: latest
    containerd:
      version: "1.7.x"
    minio:
      version: "2025-10-15T17-29-55Z"
    kotsadm:
      version: "1.131.5"
    velero:
      version: "1.16.2"
  upgradeSpec:
    kubernetes:
      version: "1.33.x"
    flannel:
      version: latest
    containerd:
      version: "1.7.x"
    minio:
      version: "2025-10-15T17-29-55Z"
    kotsadm:
      version: latest
    velero:
      version: "__testver__"
      s3Override: "__testdist__"
  preInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    # BEGIN GENERATED os-matrix: ubuntu-host-packages — edit os-matrix.yaml, run 'make generate-os-matrix'
    ubuntu_2404_install_host_packages nfs-common lvm2
    ubuntu_2604_install_host_packages nfs-common lvm2
    # END GENERATED os-matrix: ubuntu-host-packages
    rhel_9_install_host_packages lvm2 conntrack-tools socat container-selinux git
  postInstallScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    install_and_customize_kurl_integration_test_application
    # assert the repro took: kotsadm 1.131.5 must have configured the Local Volume
    # Provider despite Minio being present, otherwise this scenario is not exercising
    # the migration path
    velero_bsl_provider=$(kubectl get backupstoragelocation -n velero default -ojsonpath='{.spec.provider}')
    echo "velero bsl provider after install: ${velero_bsl_provider}"
    if ! echo "$velero_bsl_provider" | grep -q "replicated.com"; then
      echo "expected the Local Volume Provider after installing kotsadm 1.131.5, got ${velero_bsl_provider}"
      exit 1
    fi
  postUpgradeScript: |
    source /opt/kurl-testgrid/testhelpers.sh
    kubectl get pods -n velero
    # the migration must have re-pointed the default BackupStorageLocation at Minio
    velero_bsl_provider=$(kubectl get backupstoragelocation -n velero default -ojsonpath='{.spec.provider}')
    echo "velero bsl provider after upgrade: ${velero_bsl_provider}"
    if [ "$velero_bsl_provider" != "aws" ]; then
      echo "expected the aws (object store) provider after the upgrade, got ${velero_bsl_provider}"
      exit 1
    fi
    # the Local Volume Provider PVC must be gone
    if kubectl get pvc -n velero velero-internal-snapshots; then
      echo "velero-internal-snapshots PVC still exists after the upgrade"
      exit 1
    fi
    timeout 5m kubectl kots backup
    timeout 5m kubectl kots backup ls
    # if restic pods are still running, fail the test
    if kubectl get pods -n velero | grep restic; then
      echo "Restic pods still running after upgrade"
      exit 1
    fi
  unsupportedOSIDs:
    - centos-79
    - ol-79
    - ol-8x
    - amzn-20
    - ubuntu-1804
