---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: weave-net
  namespace: kube-system
spec:
  template:
    spec:
      containers:
        - name: weave
          env:
            - name: IPTABLES_BACKEND
              value: "nft"
        - name: weave-npc
          env:
            - name: IPTABLES_BACKEND
              value: "nft"
      initContainers:
        - name: weave-init
          env:
            - name: IPTABLES_BACKEND
              value: "nft"
